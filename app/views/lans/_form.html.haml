#LanModal.modal.fade{"aria-hidden" => "true", "tabindex" => "-1"}
  = form_for lan do |f|
    .modal-dialog
      .modal-content
        .modal-header
          %button.close{"data-dismiss" => "modal"}
            %span{"aria-hidden" => "true"}×
            %span.sr-only
              = t("helpers.links.close")
          %h4.modal-title= t("form.lan.title")
        .modal-body   
          .form-group
            = f.label :name
            = f.text_field :name, class: 'form-control', required: ''
          .form-group
            = f.label :description
            = f.text_area :description, class: 'form-control'
          .form-group
            = f.label :start_at
            = f.datetime_select :start_at, class: 'form-control'
          .form-group
            = f.label :end_at
            = f.datetime_select :end_at, class: 'form-control'
          .modal-footer
            %button.btn.btn-default{"data-dismiss" => "modal"}
              = t("helpers.links.close")
            = f.submit t('.save', :default => t("helpers.links.save")), :class => 'btn btn-primary'
