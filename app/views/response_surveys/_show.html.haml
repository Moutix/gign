.show-response
  = link_to survey_response.name, vote_survey_response_survey_path(survey_response.survey, survey_response), method: :post, remote: true
  - if current_user && survey_response.nb_vote(current_user) > 0
    = survey_response.nb_vote(current_user)
