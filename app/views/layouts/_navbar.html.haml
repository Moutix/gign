.navbar.navbar-default{role: "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: root_path} GiGN
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li.active
          %a{href: root_path} Accueil

      %ul.nav.navbar-nav.pull-right
        - if !current_user
          %li= link_to "S'inscrire", new_user_registration_path
          = render 'layouts/sign_through_navbar'
        - else
          %li= link_to current_user.email, users_path(current_user)
          %li= link_to "Se déconnecter", destroy_user_session_path, method: :delete
