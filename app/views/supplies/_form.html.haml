#supplyModal.modal.fade{"aria-hidden" => "true", "tabindex" => "-1"}
  = form_for supply do |f|
    .modal-dialog
      .modal-content
        .modal-header
          %button.close{"data-dismiss" => "modal"}
            %span{"aria-hidden" => "true"}×
            %span.sr-only
              = t("helpers.links.close")
          %h4.modal-title= t("form.supply.title")
        .modal-body   
          .form-group
            = f.label :name
            = f.text_field :name, class: 'form-control', required: ''
          .form-group
            = f.label :price
            = f.number_field :price, class: 'form-control'
          .form-group
            = f.label :loanable
            = f.check_box :loanable, class: 'form-control'
          .form-group
            = f.label :description
            = f.text_area :description, class: 'form-control'
          .modal-footer
            %button.btn.btn-default{"data-dismiss" => "modal"}
              = t("helpers.links.close")
            = f.submit t('.save', :default => t("helpers.links.save")), :class => 'btn btn-primary'
