%h1
  Stream de
  = link_user nil, @user

- if @user.pseudo
  = render 'stream_js', container: "stream_user_#{@user.id}", time_url: "#{GLOBALCONSTANT::STREAM_TIME_URL}", mpd_url: "#{GLOBALCONSTANT::STREAM_DASH_URL}/#{@user.pseudo}.mpd"
- else
  .alert.alert-danger
    %p Vous devez avoir un pseudo afin d'accéder à la fonction de streaming

%video#stream[@user]{controls: "true"}
