<script>
var player, video, source, context;

video = document.querySelector('#<%=container%>');
context = new Dash.di.DashContext();
player = new MediaPlayer(context);
player.addUTCTimingSource("schemeIdUri:urn:mpeg:dash:utc:http-xsdate:2014", "value:<%=url%>");
player.setLiveDelayFragmentCount(0);
player.startup();
player.attachView(video);
player.setAutoPlay(true);
player.attachSource("<%=twitch_mpd%>");
</script>
